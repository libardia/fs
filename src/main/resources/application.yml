spring:
  servlet:
    multipart:
      max-file-size: 10GB
      max-request-size: 10GB
  h2:
    console:
      enabled: true
  http:
    converters:
      preferred-json-mapper: gson
  datasource:
    url: jdbc:h2:~/db/test
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 80